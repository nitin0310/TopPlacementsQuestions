package question16;

public class UsingHashMap {
/*
 we will gonna add level as key and maxSum at that level
 recursively check at every level
 if maxSum is greater than that key value,then update it
 
 O(n) time						O(n) space
 */
}
